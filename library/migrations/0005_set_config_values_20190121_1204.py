# Generated by Django 2.1.5 on 2019-01-19 15:11

from django.db import migrations

def reset_config_information(apps, schema_editor):
    # We can't import the Configuration model directly as it may be a newer
    # version than this migration expects. We use the historical version.
    Configuration = apps.get_model('library', 'Configuration')

    if Configuration.objects.all().exists():
        config = Configuration.objects.get()
    else:
        config = Configuration()

    config.maxbooksonloan = 5
    config.save()

class Migration(migrations.Migration):

    dependencies = [
        ('library', '0004_merge_20190119_1511'),
    ]

    operations = [
        migrations.RunPython(reset_config_information),
    ]
